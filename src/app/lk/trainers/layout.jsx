export const metadata = {
  title: 'Тренеры | GYMYX',
  description: 'Тренеры | GYMYX',
  manifest: '/manifest.json',
};

const AccountTrainers = ({ children }) => {
  return <div>{children}</div>;
};

export default AccountTrainers;
