export const metadata = {
  title: 'Записаться на тренировку | GYMYX',
  description: 'Записаться на тренировку | GYMYX',
  manifest: '/manifest.json',
};

const AccountBooking = ({ children }) => {
  return <div>{children}</div>;
};

export default AccountBooking;
