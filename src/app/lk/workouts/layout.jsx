export const metadata = {
  title: 'Мои тренировки | GYMYX',
  description: 'Мои тренировки | GYMYX',
  manifest: '/manifest.json',
};

const AccountWorkouts = ({ children }) => {
  return <div>{children}</div>;
};

export default AccountWorkouts;
