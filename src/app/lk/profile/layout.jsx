export const metadata = {
  title: 'Личный кабинет | GYMYX',
  description: 'Личный кабинет пользователя | GYMYX',
  manifest: '/manifest.json',
};

const AccountProfile = ({ children }) => {
  return <div>{children}</div>;
};

export default AccountProfile;
